# ============================================
# Outreach CRM API - Environment
# ============================================
# Copy this file to .env and fill in real values.
# Do NOT commit .env to version control.

# --- Database ---
# PostgreSQL connection string (Neon recommended for production)
# Falls back to local SQLite if unset
DATABASE_URL=postgresql://user:password@host:5432/dbname?sslmode=require

# --- Authentication ---
# Secret key for signing JWT tokens
JWT_SECRET=your-jwt-secret-change-in-production

# Pre-hashed bcrypt passphrase for login
# Generate with: python -c "import bcrypt; print(bcrypt.hashpw(b'yourpass', bcrypt.gensalt()).decode())"
# Leave unset to use default dev passphrase (local dev only)
PASSPHRASE_HASH=your-bcrypt-hash-here

# --- Email (SMTP via Gmail) ---
# Gmail address used as the sender for outreach emails
SMTP_EMAIL=your-email@gmail.com

# Gmail app password (NOT your regular password)
# Generate at: https://myaccount.google.com/apppasswords
SMTP_APP_PASSWORD=your-gmail-app-password

# --- Server ---
# Port for local development (Render sets PORT automatically)
PORT=8000
